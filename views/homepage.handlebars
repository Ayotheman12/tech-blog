{{#each posts as |post|}}
<div>
    <header>
        <h2>{{post.title}}</h2>
        <p>Posted by {{post.user.username}} on {{formatDate post.date_created}}</p>
    </header>
    <p>{{post.content}}</p>
</div>
{{/each}}